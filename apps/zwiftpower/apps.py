"""App configuration for the zwiftpower app."""

from django.apps import AppConfig


class ZwiftpowerConfig(AppConfig):
    """Django app configuration for the zwiftpower app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.zwiftpower'
