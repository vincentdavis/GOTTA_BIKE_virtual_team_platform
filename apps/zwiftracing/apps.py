"""App configuration for the zwiftracing app."""

from django.apps import AppConfig


class ZwiftracingConfig(AppConfig):
    """Django app configuration for the zwiftracing app."""

    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.zwiftracing'
