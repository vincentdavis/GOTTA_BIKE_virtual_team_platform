#TODO list

**Site**
[] Create github repo

**Data Connections**
[] Allow user to set a google folder id when creating a data_connection

**Profiles and Us**
[] Create frontend view to view guild vs user accounts. Like the admin view.

**verifications**
[] Create a "Race Ready" with configuration /logic based on verification records and div, divw.
[] Add a provisional status to verification records

**Permissions**
[] Create a system to manage permissions with are synced, based on or somehow integrated with Discord.
[] Implement role-based permissions for different user groups
[] Integrate with Discord API for role-based permissions
[] Define permission levels for different actions in the application
[] Document the permission system for future maintenance

**Tasks**
[] update zr recrods bot command should return the last time it was run and not allow running more than 1 time per hour.
[] update zp records bot command should return the last time it was run and not allow running more than 1 time per hour.

**Discord**
[X] Need a way to compare guild member with discord user accounts.
