#TODO list

**Site**
[X] Login Canceeled needs theme applied. `/accounts/3rdparty/login/cancelled/`

**Data Connections**
[] Allow user to set a Google folder id when creating a data_connection

**verifications**
[] Add a provisional status to verification records

**Permissions**

**Tasks**
[] update zr records bot command should return the last time it was run and not allow running more than 1 time per hour.
[] update zp records bot command should return the last time it was run and not allow running more than 1 time per hour.

**Discord**
[X] Move DBOT_AUTH_KEY to the Discord guild section of constance, site/config page. The label alt text should be "This
is
the auth key to bot uses to communicate with this apps API."
[X] Move DISCORD_URL to the Discord guild section of constance, site/config page

**USER REGISTRATION**